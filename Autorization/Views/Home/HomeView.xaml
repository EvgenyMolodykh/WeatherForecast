<UserControl x:Class="Autorization.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Autorization.Views.Home"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="3*"/>
                    <RowDefinition Height="1*"/>
                </Grid.RowDefinitions>
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel x:Name="Detals_StackPanel" Grid.Column="0" Orientation="Vertical">
                        <Label Content="{Binding Path=Location}"/>
                        <Label Content="{Binding Path=Wheather}"/>
                        <Label Content="{Binding Path=Date}"/>
                        <Label Content="{Binding Path=WeekDay}"/>
                        <Label Content="{Binding Path=MaxTemperature}"/>
                        <Label Content="{Binding Path=MinTemperature}"/>
                        <Label Content="{Binding Path=Pressure}"/>
                        <Label Content="{Binding Path=WindSpeed}"/>
                        <Label Content="{Binding Path=WindDirection}"/>
                    </StackPanel>

                    <ListBox x:Name="HourlyForecast_ListBox" Grid.Column="1">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <StackPanel Orientation="Vertical"></StackPanel>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>

                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Button>
                                    <StackPanel Orientation="Horizontal">
                                        <Label Content="{Binding Path=Time}"/>
                                        <Label Content="{Binding Path=ApparentTemperature}"/>
                                        <Label Content="{Binding Path=RelativeHumidity}"/>
                                        <Label Content="{Binding Path=WeekDay}"/>
                                        <Label Content="{Binding Path=Wheather}"/>
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>


                <ListBox x:Name="WeaterDays_ListBox" Grid.Row="1">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"></StackPanel>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Button><!--Style="{StaticResource ButtonPanelButton}" вставить ресурсы -->
                                <StackPanel Orientation="Vertical">
                                    <Label Content="{Binding Path=DataTime}"/>
                                    <Label Content="{Binding Path=MaxTemperature}"/>
                                    <Label Content="{Binding Path=MinTemperature}"/>
                                    <Label Content="{Binding Path=WeekDay}"/>
                                    <Label Content="{Binding Path=Wheather}"/>
                                </StackPanel>
                            </Button>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
      
    </Grid>
</UserControl>
